import 'package:flutter/material.dart';

class I18nPage extends StatelessWidget {
  const I18nPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('I18nPage'),
      ),
      body: null,
    );
  }
}